import {StyleSheet} from 'react-native';

export const stylesCarts = StyleSheet.create({
  footer: {
    marginTop: 16,
    alignItems: 'flex-end',
  },
  total: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 8,
  },
});
